{
  "name": "gopark",
  "version": "1.0.0",
  "private": true,
  "description": "GoPark — tìm bãi đỗ & gửi xe không vé giấy (single-service deploy)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm --prefix server install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server start"
  }
}
